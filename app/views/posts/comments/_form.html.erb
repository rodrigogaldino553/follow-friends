<%= form_with(url: post_comments_path(post)) do |form| %>
  <%= text_field "comment", "content", class: 'rounded-md shadow-md border-2 border-gray-300', placeholder: 'leave your comment!' %>
  <%= form.submit 'comment', class: 'bg-blue-600 text-white rounded-md px-4 py-2' %>
<% end %>
